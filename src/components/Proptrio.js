const Propsss = ({firstName, lastName, bandName}) => {
    return (
    <>
        <h1>{firstName}</h1>
        <h2>{lastName}</h2>
        <h3>{bandName}</h3>    
    </>
    )
}

export default Propsss